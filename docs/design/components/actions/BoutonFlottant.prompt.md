Bouton flottant vert « Proposer », seul élément vert pastel d'action. Placé par `Ecran` (prop `flottant`) à 20 px du bord droit, 100 px du bas, au-dessus de la barre de navigation.

```jsx
<BoutonFlottant onClick={proposer} />
```