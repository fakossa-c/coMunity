Logo officiel coMunity (image embarquée, aucun fichier à copier). Ne pas le redessiner ni le recolorer : deux variantes seulement.

```jsx
<Logo hauteur={24} />                 {/* en-tête des écrans principaux */}
<Logo variante="inverse" hauteur={40} /> {/* sur terre cuite ou marine */}
```

Fichiers bruts : `assets/logo.png`, `assets/logo-inverse.png`, `assets/symbole.png` (le M seul, pour icône d'app).