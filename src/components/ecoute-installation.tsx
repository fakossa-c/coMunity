"use client";

import "@/lib/installation";

/**
 * N'affiche rien : charge sur chaque page l'écoute de l'invite d'installation de Chrome,
 * pour qu'elle serve encore quand le résident rejoint l'accueil depuis une autre page.
 */
export function EcouteInstallation() {
  return null;
}
